{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}
{# set variables in view template#}
{# {% set users = [{name: 'John'}, {name: 'Susan'}, {name: 'Robert'}] %}
{% set string = "some string \n from \n database"%} #}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Home Projects/example_2/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Home Projects/example_2/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>

    {{ render(controller(
        'App\\Controller\\DefaultController::mostPopularArticles',
        { 'count':3 }
    ))}}



    {{ app.user }} </br>
    {{ app.request.get('param') }} </br>
    {{ app.environment }} </br>
    {{ dump(app.session) }}

    {# {% for user in users  %}
        {% if user.name == 'Susan' %}
            <p>{{user.name}}</p>
        {% else %}
            No users where found
        {% endif %}
    {% endfor %} #}

   {# {{ include('includes/subviwe.html.twig', {'users': users})}}
    {% for i in 1..10 %}
        <div class="{{ cycle(['even','odd'], i) }}">
            {{ i }}
        </div>
    {% endfor %} #}

    {# call route #}
    {# {{ path("home") }}</br>
    {{ url("home") }}</br>
    {{ asset('images/logo.png') }}</br>
    {{ absolute_url(asset('images/logo.png')) }}</br>
    {{string|escape('html')}}
    <p>Is a global variable {{ga_var}}</p> #}
</div>
{# <div>
<img src="{{ absolute_url(asset('images/logo.png')) }}" alt="Symfony 4 course" />
</div> #}

{% endblock %}
{# {% block javascripts %}
{{ parent() }}
<!-- <script>
   // alert(2);
    var route = "{{ string|escape('js') }}";
    alert(route);
</script> -->

{% endblock %} #}
